sometimes you need a simple ls multiple times

### Installation

```bash
$ npm install ls-x
```

##Example USAGE

```bash
## $ls -a && ls -a && ls -a && ls -a && ls -a
lsx 5 .

lsx <number> <directory>

```

### LICENSE

MIT
